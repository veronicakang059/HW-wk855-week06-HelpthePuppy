using System;
using System.Collections;
using System.Collections.Generic;
using UnityEditor.Experimental;
using UnityEngine;

public class ClickActive : MonoBehaviour
{
    // Start is called before the first frame update
    public float floatSpeed = (float) 0.2;
    public bool mouseClicked = false;

    void Start()
    {
    public float originalY = Transform.localPosition.y;
        
    }

    // Update is called once per frame
    void Update()
    {
        if(mou)
       
    }

    private void OnMouseDown()
    {
        mouseClicked = true;
    }

    void Floating()
    {
        float yMovement = (float) floatSpeed * Time.deltaTime;
        var originalY = yMovement;
        transform.position = new Vector3(0, originalY, 0);
    }
}
